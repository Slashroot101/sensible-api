const logger = require('../../logger');
const {ConfigStore} = require('../../database/models');

module.exports = async (fastify, opts) => {
    fastify.post('/', {}, async (req, reply) => {

    });

    fastify.patch('/', {}, async (req, reply) => {

    });
}